import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root=
ReactDOM(document.getElementByld(root));
root.render(
    <React.StrictMode>
        <App/>
    </React.StrictMode>
);

const Navbar=()=>{
    return(
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/">About</a></li>
                <li><a href="/">Portfolio</a></li>
            </ul>
        </nav>
    )
};